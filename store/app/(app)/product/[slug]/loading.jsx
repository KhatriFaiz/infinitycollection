import { Container } from "@mui/material";

const Loading = () => {
  return (
    <Container sx={{ paddingBlock: 5, textAlign: "center" }}>
      <div>Loading Product ...</div>
    </Container>
  );
};

export default Loading;
